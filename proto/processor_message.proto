syntax = "proto3";

option go_package = ".;pb";

message CPU {
    // brand of the CPU
    string brand = 1;
    // name of the CPU
    string name = 2;
    uint32 number_cores = 3;
    uint32 number_threads = 4;
    double min_ghz = 5;
    double max_ghz = 6;
}